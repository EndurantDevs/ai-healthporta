name: content-guard

on:
  pull_request:
  push:
    branches: [main]

jobs:
  content-guard:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Verify public-only content policy
        run: python3 scripts/content_guard.py
