# Provider Plugins

This folder contains client-specific plugin artifacts.

## Structure

- `providers/claude/plugin/` for Claude plugin metadata and MCP config.
- `providers/cursor/plugin/` for Cursor plugin metadata and MCP config.

## Skills

`providers/*/plugin/skills/` may contain public-only skill files for that client.
Do not store internal runbooks or private operational instructions.
